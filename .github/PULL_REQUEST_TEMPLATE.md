## What this changes

<!-- One or two sentences. If it closes an issue, say "Closes #123". -->

## Why

<!-- The problem, not the patch. Skip if the title says it. -->

## Checklist

- [ ] `bun run lint`, `bunx tsc --noEmit`, `bun run test`, and `bun run build` pass
- [ ] User-facing strings added to **both** `messages/en.json` and `messages/es.json`
- [ ] New behaviour has a test; a bug fix has a regression test
- [ ] Commits are signed off (`git commit -s`) — see CONTRIBUTING.md
- [ ] No manuscript text is logged, cached, or persisted anywhere new

## Anything reviewers should look at first?

<!-- Trade-offs you are unsure about, or a file to start in. -->
